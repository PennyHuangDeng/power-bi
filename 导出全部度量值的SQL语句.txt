select
MEASURE_NAME,
EXPRESSION
from $SYSTEM.MDSCHEMA_MEASURES
where MEASURE_AGGREGATOR = 0
order by MEASUREGROUP_NAME


使用方法：先用POWER BI打开要导出的度量值的案例，然后打开DAX STUDIO，选择PBI/SSDT 
MODEL-CONNECT-然后复制上面一段到面板-OUTPUT(FILE)-RUN